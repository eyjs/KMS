{
  "installCommand": "cd packages/shared && npm install && npm run build && cd ../web && npm install",
  "buildCommand": "cd packages/web && npx vue-tsc -b && npx vite build",
  "outputDirectory": "packages/web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://kms.joonbi.co.kr/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
