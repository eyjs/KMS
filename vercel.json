{
  "installCommand": "npm install",
  "buildCommand": "bash -c 'ROOT=$PWD && cd $ROOT/packages/shared && npx tsc && cd $ROOT/packages/web && npx vue-tsc -b && npx vite build --outDir $ROOT/dist'",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://kms.joonbi.co.kr/api/:path*" }
  ]
}
