{
  "buildCommand": "pnpm --filter @kms/shared build && pnpm --filter @kms/web build",
  "outputDirectory": "packages/web/dist",
  "installCommand": "pnpm install",
  "framework": "vue",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://kms.joonbi.co.kr/api/:path*" }
  ]
}
