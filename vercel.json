{
  "buildCommand": "pnpm --filter @kms/shared build && pnpm --filter @kms/web build && cp -r packages/web/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://kms.joonbi.co.kr/api/:path*" }
  ]
}
