{
  "buildCommand": "cd packages/shared && npx tsc && cd ../web && npx vue-tsc -b && npx vite build --outDir ../../dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://kms.joonbi.co.kr/api/:path*" }
  ]
}
