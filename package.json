{
  "name": "kms",
  "private": true,
  "scripts": {
    "dev:api": "npm --prefix packages/api run dev",
    "dev:web": "npm --prefix packages/web run dev",
    "build:shared": "npm --prefix packages/shared run build",
    "build:api": "npm run build:shared && npm --prefix packages/api run build",
    "build:web": "npm run build:shared && npm --prefix packages/web run build",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:migrate": "npm --prefix packages/api run prisma migrate dev",
    "db:seed": "npm --prefix packages/api run prisma db seed",
    "db:studio": "npm --prefix packages/api run prisma studio"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
